       # ========================================================
        # 🔥 [新增功能] 規格缺漏檢查 (Missing Spec Check)
        # ========================================================
        # 條件：有標題(title存在) 且 沒規格(raw_spec為空)
        # 排除：如果標題本身就是空的，或者包含常見雜訊(如 "備註")則不抓
        if title and not raw_spec and len(title) > 1:
            # 建立錯誤卡片
            key = (page_num, raw_title, "規格缺漏")
            if key not in grouped_errors:
                grouped_errors[key] = {
                    "page": page_num, 
                    "item": raw_title, 
                    "issue_type": "⚠️規格缺漏", 
                    "common_reason": "有項目名稱，但未偵測到規格數據", 
                    "failures": [{"id": "規格欄", "val": "空白", "calc": "缺失"}],
                    "source": "🐍 工程引擎"
                }
            # 既然沒規格，後面的數值比對也不用做了，直接跳下一題
            continue 
        # ========================================================